name: Temporary auth generation helper

on:
  push:
    branches: [feat/better-auth-identity]

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - uses: oven-sh/setup-bun@v2.2.0
        with:
          bun-version-file: .bun-version
      - run: bun install --lockfile-only
      - run: bunx tsr generate
      - name: Commit generated files
        run: |
          if git diff --quiet -- bun.lock src/routeTree.gen.ts; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add bun.lock src/routeTree.gen.ts
          git commit -m "chore(auth): refresh generated auth artifacts"
          git push
